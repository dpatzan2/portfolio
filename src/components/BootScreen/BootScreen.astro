

<div id="boot-screen" class="boot-screen">
  <div class="boot-content">
    <!-- Logo -->
    <div class="ubuntu-logo">
      <img src="/assets/logo.png" alt="Logo" class="logo-img" />
    </div>

    <!-- Ubuntu Text -->
    <h1 class="ubuntu-text">dpatzan</h1>
    
    <!-- Loading dots animation -->
    <div class="loading-dots">
      <span class="dot"></span>
      <span class="dot"></span>
      <span class="dot"></span>
      <span class="dot"></span>
      <span class="dot"></span>
    </div>

    <!-- Boot messages (optional, for more realism) -->
    <div class="boot-messages">
      <p class="boot-msg" style="--delay: 0.8s">Iniciando kernel...</p>
      <p class="boot-msg" style="--delay: 1.6s">Cargando módulos del sistema...</p>
      <p class="boot-msg" style="--delay: 2.5s">Iniciando servicios...</p>
      <p class="boot-msg" style="--delay: 3.5s">Preparando entorno de escritorio...</p>
      <p class="boot-msg" style="--delay: 4.2s">¡Bienvenido!</p>
    </div>
  </div>
</div>

<style>
  .boot-screen {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background: linear-gradient(135deg, #2C001E 0%, #5E2750 50%, #77216F 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 99999;
    opacity: 1;
    transition: opacity 0.8s ease-out, visibility 0.8s ease-out;
  }

  .boot-screen.hidden {
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
  }

  .boot-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 2rem;
  }

  .ubuntu-logo {
    animation: fadeInScale 0.8s ease-out forwards;
    opacity: 0;
  }

  .logo-img {
    width: 150px;
    height: 150px;
    object-fit: contain;
    filter: drop-shadow(0 0 30px rgba(233, 84, 32, 0.4));
  }

  .ubuntu-text {
    font-family: 'Ubuntu', sans-serif;
    font-size: 3.5rem;
    font-weight: 300;
    color: #FFFFFF;
    letter-spacing: 0.1em;
    margin: 0;
    animation: fadeIn 0.6s ease-out 0.3s forwards;
    opacity: 0;
  }

  .loading-dots {
    display: flex;
    gap: 12px;
    animation: fadeIn 0.5s ease-out 0.6s forwards;
    opacity: 0;
  }

  .dot {
    width: 12px;
    height: 12px;
    background-color: #FFFFFF;
    border-radius: 50%;
    animation: dotPulse 1.4s infinite;
  }

  .dot:nth-child(1) { animation-delay: 0s; }
  .dot:nth-child(2) { animation-delay: 0.2s; }
  .dot:nth-child(3) { animation-delay: 0.4s; }
  .dot:nth-child(4) { animation-delay: 0.6s; }
  .dot:nth-child(5) { animation-delay: 0.8s; }

  .boot-messages {
    position: absolute;
    bottom: 60px;
    left: 50%;
    transform: translateX(-50%);
    text-align: center;
    font-family: 'Ubuntu Mono', monospace;
    font-size: 0.75rem;
    color: rgba(255, 255, 255, 0.5);
  }

  .boot-msg {
    margin: 0.3rem 0;
    opacity: 0;
    animation: bootMsgAppear 0.3s ease-out forwards;
    animation-delay: var(--delay);
  }

  @keyframes fadeInScale {
    0% {
      opacity: 0;
      transform: scale(0.5);
    }
    100% {
      opacity: 1;
      transform: scale(1);
    }
  }

  @keyframes fadeIn {
    to {
      opacity: 1;
    }
  }

  @keyframes dotPulse {
    0%, 100% {
      opacity: 0.3;
      transform: scale(0.8);
    }
    50% {
      opacity: 1;
      transform: scale(1);
    }
  }

  @keyframes bootMsgAppear {
    to {
      opacity: 0.5;
    }
  }

  @media (max-width: 640px) {
    .logo-img {
      width: 100px;
      height: 100px;
    }

    .ubuntu-text {
      font-size: 2.5rem;
    }

    .dot {
      width: 10px;
      height: 10px;
    }

    .boot-messages {
      font-size: 0.65rem;
      bottom: 40px;
      width: 90%;
    }
  }
</style>

<script>
  // Boot screen timing - Show boot animation then fade out
  (function() {
    const bootScreen = document.getElementById('boot-screen');
    
    if (!bootScreen) return;
    
    // Always show the boot animation on page load
    // Use setTimeout to ensure DOM is ready
    setTimeout(() => {
      bootScreen.classList.add('hidden');
      
      // Remove from DOM after fade animation completes
      setTimeout(() => {
        bootScreen.remove();
      }, 800);
    }, 5000); // Boot duration: 5 seconds
  })();
</script>
